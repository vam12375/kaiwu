"""行动计划导出模块 —— 将各 node 输出蒸馏为可执行的品牌策略行动计划。

职责：从 _node_output_cache 读取已完成的节点输出，调用 LLM 蒸馏生成 15-25 页
结构化的品牌策略行动计划 Markdown 文件。
"""

import re
from server.llm_client import call_deepseek

# 各 node 输出的摘要长度上限（避免 prompt 过长）
NODE_SUMMARY_MAX = 3000

# node → 行动计划中的角色
NODE_ROLES = {
    "node0": "创始人诊断（预算/团队/动机/约束）→ 行动计划 Part 3 资源与保障",
    "node1": "市场调研（规模/竞品/用户/机会）→ 行动计划 Part 1 市场机会与竞争位置",
    "node1.5": "品牌设计（人群精神/品牌屋/Logo提示词）→ 行动计划 Part 1 品牌核心承诺",
    "node2": "商业方案（商业模式/品牌命名/品牌故事/定价/盈亏平衡）→ 行动计划 Part 1 策略摘要 + Part 3 预算",
    "node3": "产品设计（SKU规划/定价/成本/启动方案）→ 行动计划 Part 3 产品与内容日历",
    "node4": "营销方案（5A漏斗/4媒介/3个月内容策略）→ 行动计划 Part 2 半年行动路线图",
    "node5": "自媒体文案（品牌/创始人/产品/客户故事 + 8周发布节奏）→ 行动计划 Part 2 内容产出计划",
}

ACTION_PLAN_PROMPT = """你是品牌策略执行顾问。你的任务是将多份品牌分析报告**蒸馏**为一份 15-25 页的《品牌策略行动计划》。

## ⛔ 核心原则

1. **不重新分析**：所有分析结论已经在上游报告中完成。你只提取和翻译，不做新的分析。
2. **数字必须具体**：不说"尽快"，说"第3周前"；不说"适当预算"，说"15-20万"
3. **语言从顾问的翻译成团队的**：不说"建议品牌采取…"，说"我们要…"、"你的团队需要…"
4. **留白给团队填写**：关键指标看板、月度复盘等页面预留空白表格
5. **不写"为什么"，只写"做什么"**：分析过程留在上游报告中

## 输入数据来自以下节点（可能有缺失，缺失标注即可）

{node_descriptions}

---

## 输出模板（严格按此结构，每页用 `### 第X页｜标题` 标记）

```
# 品牌策略行动计划 · {{品牌名}}

### 第1页｜封面
- 品牌名
- "品牌策略行动计划"
- 时间范围：202X年X月 — 202X年X月
- 日期 + 版本号 V1.0

──────────────────────────────────────
# 第一部分：策略摘要
──────────────────────────────────────

### 第2页｜一句话战略
- 生态位定义（一句话，来自市场调研结论）
- 品牌定位语（压缩版）
- 品牌核心特质（3个关键词）
- 主力广告语

### 第3页｜品牌核心承诺
- 使命
- 品牌故事一句话（初心→态度→承诺）
- 品牌价值观 3 条（每条一句话）
- 目标用户一句话画像

### 第4页｜市场机会与竞争位置
- 市场规模 + 增速（1 个核心数字）
- 结构性缺口（1 句话）
- 竞品格局简图（文字描述 Top 3 竞品位置）
- 关键差异化维度（3 个要点）

### 第5页｜核心发现与策略逻辑
- 从市场调研到战略定位的推导链路（精简版，3-5 句话）
- 为什么选择这个定位方法（2-3 句话 + 对标案例）
- 品牌初期三原则（聚焦什么 / 立住什么 / 控制什么）

──────────────────────────────────────
# 第二部分：行动计划
──────────────────────────────────────

### 第6页｜半年行动路线图（总览）
用表格呈现：

| 月份 | 核心目标 | 关键动作 | 门禁条件 | 交付物 |
|------|---------|---------|---------|--------|
| 第1月 | | | | |
| 第2月 | | | | |
| 第3月 | | | | |
| 第4月 | | | | |
| 第5月 | | | | |
| 第6月 | | | | |

### 第7页｜第1-2个月：冷启动 + 信任建立
- 本月核心目标（1 句话）
- 具体动作（5-8 项，每项含负责人/交付物/完成标准）
- 本周就能做的 3 件事
- **本月不做什么**（明确列出）

### 第8页｜第3-4个月：内容体系搭建 + 种子用户积累
- 本月核心目标
- 具体动作（5-8 项）
- 首批种子用户招募计划
- MVP 上线准备清单

### 第9页｜第5-6个月：社群启动 + 产品迭代
- 本月核心目标
- 具体动作（5-8 项）
- 社群启动计划（初期规模/运营方式）
- 首波用户反馈→产品迭代清单

### 第10页｜6-12个月季度里程碑
| 季度 | 核心任务 | 关键结果 | 验证指标 |
|------|---------|---------|---------|
| Q3（7-9月） | | | |
| Q4（10-12月） | | | |

### 第11-12页｜关键指标看板
三类指标：信任指标 / 增长指标 / 商业指标

| 指标类型 | 指标名 | 第1月目标 | 第3月目标 | 第6月目标 | 实际值（待填） |
|---------|--------|----------|----------|----------|------------|
| 信任 | | | | | ___ |
| 增长 | | | | | ___ |
| 商业 | | | | | ___ |

- 北极星指标定义 + 测量方式
- Go/No-Go 条件：什么情况下调整方向？什么情况下加速投入？
- 季度复盘模板（4 个问题）：什么做对了 / 什么做错了 / 学到了什么 / 下季度改什么

──────────────────────────────────────
# 第三部分：资源与保障
──────────────────────────────────────

### 第13页｜预算分配方案
- 总预算量级
- 6 个月预算分配明细表：

| 预算项目 | 占比 | 金额范围 | 备注 |
|---------|------|---------|------|
| 产品研发与迭代 | | | |
| 内容生产 | | | |
| 社群运营 | | | |
| 营销推广 | | | |
| 机动预留 | | | |
| **合计** | 100% | | |

- 明确标注**不花钱的地方**（初期不碰的支出）

### 第14页｜团队配置与招聘计划
- 当前团队规模与结构
- 最小可行团队配置建议
- 关键岗位缺口 + 招聘优先级（P0/P1/P2）
- 6 个月内建议到岗时间线
- 外包 vs 自建判断

### 第15页｜风险应对预案
Top 5 风险：

| 风险 | 预警信号 | 应对动作 |
|------|---------|---------|
| 1. | | |
| 2. | | |
| 3. | | |
| 4. | | |
| 5. | | |

- "如果只有一半预算"的极端情景推演
- "如果 3 个月后验证失败"的 Plan B

──────────────────────────────────────
# 附录
──────────────────────────────────────

### 第16页｜关键资源清单
- 推荐工具（5-8 个，标注工具名+用途+费用）
- 建议关注的对标品牌（3-5 个）

### 第17页｜下一步
- "如果对以上计划有任何疑问，建议回顾各节点分析报告"
- 下次复盘时间建议

---

以上为《品牌策略行动计划》完整输出。
```

## 输出规则

1. 每个 `### 第X页｜` 标题代表一页，完整输出每一页内容
2. 缺失数据的节点，在对应位置标注"待完成XX分析后补充"，不要编造
3. 所有表格单元格必须填充，无法从数据中提取的标注[待确认]
4. 金钱数字用"元/万元"标注单位
5. 输出纯 Markdown，不要用代码块包裹
"""


def build_action_plan_md(cached_nodes: dict, history: list = None) -> str:
    """从各 node 输出缓存蒸馏生成行动计划 Markdown。

    Args:
        cached_nodes: {node_id: ai_text} 字典，来自 _node_output_cache
        history: 对话历史（用于提取品牌名等上下文）

    Returns:
        结构化的行动计划 Markdown 文本
    """
    if history is None:
        history = []

    # 构建节点描述
    node_sections = []
    for node_id, role in NODE_ROLES.items():
        node_sections.append(f"- {node_id}（{role}）")

    # 拼接各节点输出摘要
    context_parts = []
    for node_id, ai_text in cached_nodes.items():
        if not ai_text or not ai_text.strip():
            continue
        role = NODE_ROLES.get(node_id, node_id)
        snippet = ai_text[:NODE_SUMMARY_MAX]
        context_parts.append(f"### {node_id} 输出摘要（{role}）\n\n{snippet}\n")

    node_descriptions = "\n".join(node_sections)
    context_text = "\n---\n".join(context_parts) if context_parts else "（暂无节点输出缓存）"

    # 尝试提取品牌名
    brand_name = _extract_brand_name(cached_nodes, history)

    user_message = f"""以下是从各分析节点提取的输出数据：

{context_text}

---

请根据这些数据，按《品牌策略行动计划》模板生成完整的行动计划 Markdown。
品牌名：{brand_name or "（请从数据分析中提取）"}
注意：缺失的节点在对应位置标注"待完成XX分析后补充"，禁止编造数据。"""

    try:
        result = call_deepseek(ACTION_PLAN_PROMPT, user_message, timeout=120, max_tokens=8192)
        if result and len(result) > 200:
            return result
    except Exception as e:
        print(f"[ACTION_PLAN] LLM call failed: {e}", flush=True)

    # 降级：返回基础模板
    return _build_fallback_plan(cached_nodes, brand_name)


def _extract_brand_name(cached_nodes: dict, history: list = None) -> str:
    """尝试从缓存或历史中提取品牌名。"""
    if history is None:
        history = []

    # 优先从 node2（商业方案）的品牌命名部分提取
    for node_id in ("node2", "node1.5", "node0"):
        text = cached_nodes.get(node_id, "")
        if not text:
            continue
        m = re.search(r'(?:品牌名|品牌名称)[：:]\s*(?:[*_]*)(.+?)(?:[*_]*)(?:\n|$)', text)
        if m:
            return m.group(1).strip()[:20]
        m = re.search(r'\|\s*\d+\s*\|\s*([^|]+)\s*\|', text)
        if m:
            name = m.group(1).strip()
            if len(name) < 15:
                return name

    # 从历史消息中尝试
    for m in history:
        content = m.get("content", "")
        bm = re.search(r'(?:品牌名|品牌)[：:]\s*(.+?)(?:\n|$)', content)
        if bm:
            return bm.group(1).strip()[:20]

    return ""


def _build_fallback_plan(cached_nodes: dict, brand_name: str = "") -> str:
    """当 LLM 调用失败时，生成基础模板。"""
    name = brand_name or "（请补充品牌名）"
    available_nodes = [n for n, t in cached_nodes.items() if t and t.strip()]
    node_status = "\n".join([
        f"- {node_id}：{'✅ 已完成' if node_id in available_nodes else '❌ 待完成'}"
        for node_id in NODE_ROLES
    ])

    return f"""# 品牌策略行动计划 · {name}

> ⚠️ AI 蒸馏服务暂时不可用，以下为基础模板。请在各节点分析完成后重新生成。

## 当前节点状态

{node_status}

---

## 策略摘要

（待 LLM 蒸馏生成。请在各节点分析完成后，重新触发"导出行动计划"获取完整内容。）

## 行动计划

（待生成）

## 资源与保障

（待生成）

## 附录

（待生成）

---

以上为《品牌策略行动计划》基础模板。
"""
